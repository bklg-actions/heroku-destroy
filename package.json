{
  "name": "heroku-auth",
  "version": "1.0.0",
  "description": "GitHub Action that adds your Heroku credentials to ~/.netrc for other jobs in your workflow.",
  "main": "index.js",
  "scripts": {
    "lint": "eslint .",
    "build": "ncc build lib/index.js -o dist --source-map --license licenses.txt",
    "test": "jest",
    "all": "npm run lint && npm run prepare && npm run test"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/bklog-actions/heroku-auth.git"
  },
  "keywords": [
    "GitHub",
    "Actions",
    "JavaScript"
  ],
  "author": "",
  "license": "MIT",
  "bugs": {
    "url": "https://github.com/bklog-actions/heroku-auth/issues"
  },
  "homepage": "https://github.com/bklog-actions/heroku-auth#readme",
  "dependencies": {
    "@actions/core": "^1.2.5"
  },
  "devDependencies": {
    "@vercel/ncc": "^0.28.6",
    "eslint": "^7.4.0",
    "jest": "^26.6.3"
  }
}
